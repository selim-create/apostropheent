{
  "name": "apostropheent",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "sync:legacy": "node scripts/sync-legacy-assets.mjs",
    "predev": "npm run sync:legacy",
    "dev": "next dev",
    "prebuild": "npm run sync:legacy",
    "build": "next build",
    "start": "next start"
  },
  "dependencies": {
    "next": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "@types/node": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "typescript": "latest"
  }
}
